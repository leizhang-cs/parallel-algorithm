To run the program:
1. make
2. ./ssort [n_elements]
3. ./ssort_seq [n_elements]
4. ./ssort_na [n_elements]

ssort is the optimized version
ssort_seq is the sequential version
ssort_na is the naive parallel version
